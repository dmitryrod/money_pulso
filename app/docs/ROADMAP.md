# Roadmap

Планы наперёд (не дублирует [CHANGELOG.md](CHANGELOG.md)).

## Возможные направления

- Опциональная **жёсткая авторизация** в админке (логин/пароль из env уже есть в конфиге, провайдер сейчас пропускает всех).
- Расширение метрик/health на фоновой задаче `runtime_support` (сейчас заглушка).
- Приведение в порядок или удаление неиспользуемого `screener/producer.py`, если он не планируется к возврату в пайплайн.
- Dev-зависимости (`pytest` и др.) закрепить в `pyproject.toml` при необходимости.

Приоритеты задаёт владелец продукта; этот файл обновлять при смене планов.
